version: "2"
sql:
  - schema: "./db/migrations"
    queries: "./db/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "./internal/storages/db/"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
